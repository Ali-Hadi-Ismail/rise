import 'package:flutter/material.dart';

class ProjectDisplayScreen extends StatelessWidget {
  const ProjectDisplayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
